---
type: agent_context
project: terrain
title: Agent Architecture Context
source: .
---

## Containers| Container | Type | Path | Purpose |
|-----------|------|------|---------|
| Desktop App | Tauri + Svelte | `src-tauri/`, `src/` | GUI: AskBar, KnowledgePanel, SourceDrawer |
| CLI Tool | Binary | `crates/terrain-cli/` | CLI commands: assets, env, knowledge, tools |
| Core Library | Rust crate | `crates/terrain-core/` |所有业务逻辑、IPC绑定、知识生成引擎 |
| Agent Layer | Rust crate | `crates/terrain-agent/` | ACP协议、Agent上下文生成、聊天追踪器 |

### Layer Dependencies- **src-tauri** → invokes commands on **core** (via Tauri IPC)- **crates/terrain-core** → depends on **terrain-agent**, **ts-rs** for bindings- **crates/terrain-ts-export** → generates `.js` from Rust types via `ts-rs`- **npm/packages/** → packaged native binaries (darwin-arm64, win32-x64)###关键 IPC通道 (`crates/terrain-core/src/ts_ipc.rs`)

| Command | Direction | Purpose |
|---------|-----------|---------|
| `check_llm` | App→Core | LLM provider readiness check |
| `add_project` | App→Core | Register project for scanning |
| `ask_question` | App→Core | Execute knowledge search + answer |##模块地图| Module | Responsibility | Primary Paths |
|--------|---------------|---------------|
| **项目初始化** | Git扫描 →注册 → repomix打包 | `crates/terrain-core/src/project.rs`, `agent_context.rs` |
| **Litho文档生成** |研究→编排→输出人类可读知识 | `crates/terrain-agent/src/litho.rs`, `context_generator.rs` |
| **DeepWiki问答** |搜索知识库 → LLM推理 →回答 | `crates/terrain-core/src/search.rs`, chat模块 |
| **ACP协议层** | Agent编码助手通信协议 | `crates/terrain-agent/src/acp.rs`, `acp_agent.rs` |
| **环境管理** | .env集成、Catalog、freshness追踪 | `crates/terrain-core/src/assets/env/*.rs` |
| **知识资产存储** | `.terrain/knowledge/`, metadata | `src/lib/knowledgeDoc.ts`, `project_meta.rs` |
| **SDD工作流** | Session-driven document生成 | `crates/terrain-agent/src/sdd.rs`, `sdd-workflow-skill/SKILL.md` |
| **工具部署** |打包/分发 agent tools | `crates/terrain-core/src/bundled_tools.rs` |##核心流程###1.项目初始化流水线① Git仓库扫描 →解析目录结构②调用 LLM分析架构设计（C4模型）③生成 repomix打包文件 `agent/repomix.md`④注册到 `.terrain/projects/*.json`###2. Litho文档生成流水线① **Phase1**:预处理 —扫描源码、收集 metadata② **Phase2**:研究 — LLM分析各模块职责和依赖关系③ **Phase3**:编排 —合成 C4架构图 + Mermaid图表④ **Phase4**:输出 —生成 `knowledge/*.md` + `agent/context.md`###3. DeepWiki问答流水线①用户提问 →搜索 `.terrain/knowledge/`索引② LLM基于知识上下文进行推理③返回结构化答案（含来源引用）##技术选型- **语言**: Rust (核心) + TypeScript/Svelte (桌面前端) + Node.js (npm包分发)- **LLM Provider**: OpenAI / Anthropic / Ollama (via `adk-model` crate v1.0.0)- **桌面框架**: Tauri2.x + Svelte + Vite (build: `vite.config.ts`)- **知识图谱**: Mermaid.js图表 + Markdown文档存储- **分发平台**: darwin-arm64 / win32-x64 native binaries (npm packages)- **协议**: ACP (Agent Client Protocol) for agent coding assistant communication##系统边界

### External APIs| API | Purpose | Notes |
|-----|---------|-------|
| OpenAI / Anthropic | LLM推理（架构分析、问答） |通过 `adk-model` crate抽象 |
| Ollama (本地) |离线 LLM推理 | fallback provider |

### Storage Boundaries| Store | Path | Notes |
|-------|------|-------|
| Knowledge Assets | `.terrain/knowledge/*.md` | Litho生成的人类文档 |
| Agent Context | `.terrain/agent/context.md` | ≤14 KiB agent-facing context |
| Project Registry | `.terrain/projects/*.json` |项目元数据索引 |
| Code Graph DB | `.codegraph/codegraph.db` | SQLite代码图谱缓存 |

### Third-party / Trust Boundaries

- `preset_skills/` —预设技能（agent-context-skill, litho-documents-skill等）

- `env-catalog/skills/*.md` —环境集成技能定义

- `src-tauri/capabilities/default.json` — IPC权限白名单-不存储用户代码到远程服务，所有分析本地完成##代码映射索引| Concept | Location | Notes |
|---------|----------|-------|
|核心库根 | `crates/terrain-core/src/lib.rs` |业务逻辑入口、所有模块 re-export |
| IPC绑定生成 | `crates/terrain-ts-export/Cargo.toml` | ts-rs → JS bindings (features: ts-export, repomix) |
| ACP Agent | `crates/terrain-agent/src/acp.rs` | Agent Client Protocol实现 |
| Agent上下文生成 | `crates/terrain-core/src/assets/context_layers.rs` | context.md分层构建逻辑 |
| Litho引擎 | `crates/terrain-agent/src/litho.rs` + `context_generator.rs` |文档流水线核心 |
| SDD工作流 | `crates/terrain-agent/src/sdd.rs` | Session-driven document generation |
| DeepWiki搜索 | `crates/terrain-core/src/search.rs` |知识索引查询 |
| Git扫描 | `crates/terrain-core/src/ingest/git.rs` |仓库结构解析 |
|环境集成 | `crates/terrain-core/src/assets/env/*.rs` | .env catalog + status tracking |
| CLI Commands | `crates/terrain-cli/src/commands/tools.rs` | terrain tools CLI接口 |---**Generated by Terrain Agent Architecture Skill** ·2024```