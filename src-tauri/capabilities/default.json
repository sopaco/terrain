{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window capability for MindMesh",
  "windows": ["main"],
  "remote": {
    "urls": ["http://localhost:1420"]
  },
  "permissions": [
    "core:default",
    "dialog:default",
    "shell:default",
    "shell:allow-open"
  ]
}
