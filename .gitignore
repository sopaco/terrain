# Rust
/target/
**/*.rs.bk

# ts-rs 的默认导出目录 —— `cargo test --features ts-export` 的副产物，与
# src/lib/generated/（由 `bun run gen:types` 生成，才是前端真源）内容重复。
# 入库会让每次跑测试都产生几十个无关改动，并挤占增量保鲜的变更文件预算。
crates/*/bindings/

# Node
node_modules/
dist/
.svelte-kit/

# Tauri
src-tauri/target/
src-tauri/binaries/*
!src-tauri/binaries/.gitkeep
src-tauri/bundled-resources/

# Env
.env
.env.*

# OS / IDE
.DS_Store
.idea/
.vscode/

# Logs
*.log

# Terrain — 知识资产的 Git 跟随策略声明在 .terrain/.gitignore 与
# .terrain/.gitattributes（由 Terrain 生成维护、随仓库分发），此处不再重复。

# Bundled agent binaries (installed to ~/.terrain/bin /, not versioned)
packages/codegraph/darwin-arm64/node

# CodeGraph local index
.codegraph

# Claude Code — 仅忽略本机配置；.claude/skills/ 由 Terrain 注入且需入库，
# 不能整目录忽略（否则新增 skill 会被 `git add .` 静默漏掉）
.claude/settings.local.json
.claude/launch.json
