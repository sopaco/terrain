openapi: "3.0.3"
info:
  title: Demo API
  version: "1.0"
paths:
  /health:
    get:
      operationId: healthCheck
      summary: Health check endpoint
      responses:
        "200":
          description: OK
